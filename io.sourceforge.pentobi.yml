app-id: io.sourceforge.pentobi
runtime: org.kde.Platform
runtime-version: '6.7'
sdk: org.kde.Sdk
command: pentobi
rename-icon: pentobi
finish-args:
  - --device=dri
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11

modules:
  - name: pentobi
    buildsystem: cmake
    sources:
      - type: git
        url: https://github.com/enz/pentobi.git
        tag: v26.1
        commit: 9172894e2d2064237726a20d1803c0a528008ef3
        x-checker-data:
          type: git
          tag-pattern: ^(v[\d.]+)$

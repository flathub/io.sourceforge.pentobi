app-id: io.sourceforge.pentobi
runtime: org.kde.Platform
runtime-version: 6.4
sdk: org.kde.Sdk
command: pentobi
rename-icon: pentobi
finish-args:
  - --device=dri
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio

modules:
  - name: pentobi
    buildsystem: cmake
    sources:
      - type: git
        url: https://github.com/enz/pentobi.git
        tag: v23.0
        commit: 384020a32673a95a01a4d880b83043790c358ff2
        x-checker-data:
          type: git
          tag-pattern: ^(v[\d.]+)$
